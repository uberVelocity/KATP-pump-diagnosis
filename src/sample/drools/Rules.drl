package sample.model;
dialect  "mvel"
import Pump;
import Model;

rule "Rules"
    when
        item : Pump(item.isVibrating() == true)
    then
        System.out.println("The pump is vibrating");
end

